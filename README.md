# AmbedkarGPT-intern-task
📘 RAG Chatbot using Hugging Face Embeddings + ChromaDB + Streamlit

This project is a Retrieval-Augmented Generation (RAG) application built with:

Hugging Face Embeddings (sentence-transformers)

ChromaDB (persistent vector store)

Streamlit (frontend UI)

Python + LangChain utilities

The app loads your documents, embeds them, stores vectors in ChromaDB, and answers user queries using context-aware retrieval.

🚀 Features

✅ Load and index custom text files
✅ Split documents into chunks for better retrieval
✅ Generate embeddings using Hugging Face Sentence Transformers
✅ Store vectors in a persistent ChromaDB database
✅ Ask questions in the Streamlit UI
✅ Retrieves relevant chunks → sends to LLM → generates answer
✅ Fast and lightweight (local-only)
